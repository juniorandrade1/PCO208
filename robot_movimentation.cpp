#include <bits/stdc++.h>

using namespace std;

struct Action {
  Action(){};
};

struct State {
  State(){};
};

int main() {
  int n, m; scanf("%d %d", &n, &m);
  vector< vector< State > > matrix(n, vector< State >(m));
  return 0;
}